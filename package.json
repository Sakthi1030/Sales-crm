{
  "name": "nexus-crm",
  "version": "1.0.0",
  "description": "Nexus CRM — Full-stack Sales CRM built with React + Node.js + PostgreSQL",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "seed": "cd backend && npm run seed",
    "build": "cd frontend && npm run build",
    "render-build": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "render-start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
